import 'package:flutter/material.dart';

class AppColors {
  // ── Core background / surface ───────────────────────────────────────────────
  static const background   = Color(0xFFEEF0F8); // App Background (brand)
  static const surface      = Color(0xFFFFFFFF); // pure white cards
  static const surfaceAlt   = Color(0xFFE8EAF5); // slightly tinted surface

  // ── Brand purple ────────────────────────────────────────────────────────────
  static const primary      = Color(0xFF6C5CE7); // Primary Purple (brand)
  static const primaryLight = Color(0xFFA29BFE); // Soft Purple (brand)
  static const primaryDark  = Color(0xFF5848CC);

  // ── Date Mode — Primary Purple ──────────────────────────────────────────────
  static const dateBg             = Color(0xFFEDE8FF); // light purple card bg
  static const dateGradientStart  = Color(0xFF6C5CE7); // Primary Purple
  static const dateGradientEnd    = Color(0xFF8B7CF6);
  static const dateAccent         = Color(0xFF6C5CE7);

  // ── Spice It Up — Spice Red ─────────────────────────────────────────────────
  static const couplesBg          = Color(0xFFFFEBEB); // light red card bg
  static const spiceGradientStart = Color(0xFFFF6B6B); // Spice Red (brand)
  static const spiceGradientEnd   = Color(0xFFE85555);
  static const spiceAccent        = Color(0xFFFF6B6B);

  // ── Family Time — Family Green ──────────────────────────────────────────────
  static const familyBg            = Color(0xFFE0FAF3); // light green card bg
  static const familyGradientStart = Color(0xFF00C897); // Family Green (brand)
  static const familyGradientEnd   = Color(0xFF00A87E);
  static const familyAccent        = Color(0xFF00C897);

  // ── Friends Mode — Friends Blue ─────────────────────────────────────────────
  static const friendsBg          = Color(0xFFE8F1FC); // light blue card bg
  static const sportGradientStart = Color(0xFF378ADD); // Friends Blue (brand)
  static const sportGradientEnd   = Color(0xFF2A6DBF);
  static const sportAccent        = Color(0xFF378ADD);

  // ── Text (dark on light) ────────────────────────────────────────────────────
  static const textPrimary   = Color(0xFF1A1A2E); // Dark Navy (brand)
  static const textSecondary = Color(0xFF6B7280);
  static const textMuted     = Color(0xFF9CA3AF);

  // ── Premium gold ────────────────────────────────────────────────────────────
  static const gold      = Color(0xFFF59E0B);
  static const goldLight = Color(0xFFFEF3C7);

  // ── Card surface ────────────────────────────────────────────────────────────
  static const cardWhite = Color(0xFFFFFFFF);
}
